# User feedback batch — 2026-06-18 (testing deployed build)

## Connect screen
1. On clicking Connect: REMOVE the border-beam animation on the connect button.
2. Button background becomes a GREEN PROGRESS BAR filling left→right across the button.
3. NO text in the button while loading — only a loading icon centered.
4. Interface a bit slow; the border-beam animation lags. (perf)

## Shared (revert recent additions, across editors)
5. REMOVE the "Saved · Synced" sync-status label under the center-top title (EditorTitlePill). (all editors)
13. Publish UI: REMOVE the explicit "Publish to Workshop — Unlisted" button; keep just the visibility options.
14. REMOVE the "Export project file" button + its text (title-pill + SavedProjectsList rows).
21. REMOVE the "Apply Livery…" button (skin editor).

## Faceplate editor
6. Shapes submenu in toolbar has too much right padding → auto-size width to the number of items.
7. Eraser doesn't work properly when drawing — FIX.

## Decal editor
9. Rename/repurpose the "checkerboard" view → "Default view": toggle Template view ↔ Default view, where
   Default view shows what a default/stock decal looks like in that slot.
10. Faction selector under the center title label is confusing — clarify.
11. REMOVE the keyboard-shortcut button in the top-left (undo/redo stay).
15. Description scrollbar uses the default scrollbar → use the app's custom scrollbar.
16. Decal-pack selector should only show decals that work for the SELECTED faction (faction-filter).

## Skin / texture editor
17. Opening a skin pack GLITCHES — the tank disappears. (regression)
18. New skin pack: shows winter grass then flips to summer grass; shows a "sync error" under the title. (regression)
19. The TEMPLATE selector shows only vehicles, not skin packs → it should show downloaded/available skin
    packs to apply to vehicles.
20. "Edit Texture" should use the faceplate-style editor (see item 8) — BIG.
8 + 20. "Why isn't the faceplate editor used in the decal + texture editors" → UNIFY the decal + texture
   editors onto the faceplate editor. *** MAJOR re-architecture — scope separately, not a quick fix. ***

## 3D rendering
22. Studio grid view: vehicle FULLY illuminated, NO reflections.
23. Showcase view: NO reflections, and a bit BRIGHTER.

## STATUS — all fixed except item 8/20 (building+deploying)
DONE: 1,2,3,4 (connect: beam-off-on-click, green progress fill, icon-only, perf); 5,13,14,21 (sync
label removed, publish=visibility-driven, export buttons removed, Apply-Livery removed); 6,7 (shapes
auto-width, eraser destination-out fix); 9,10,11,15 (default view, faction-selector clarity, kbd btn
removed, custom scrollbar); 16 (decal faction-filter); 17 (tank-vanish: GPU-warm pump reparenting live
mesh — fixed); 18 (winter flash intact + empty-project "nothing to sync yet" not error); 19 (template
lists installed skin packs); 22,23 (studio full-illum no-reflections; showcase brighter no-reflections).
ITEM 8 + 20 — UNIFY EDITORS: DONE (groundwork → plan → 3 phases). All 3 editors now share a layer-
compositor core (src/lib/layer-compositor/ composeLayers + GenericLayerProject) + the dark-glass
LayersPanel/PropertiesPanel. Phase 0 (extract + faceplate proving ground, golden-equal), Phase 1
(decal adopts core, export byte-identical), Phase 2 (texture: BaseDiffuse+PaintLayer → 2048² →
CanvasTexture, incremental per-dab paint path, export untouched). Suite 2084 green, tsc clean.
Plan: artifacts/respec_audit/unify_design/IMPLEMENTATION_PLAN.md. LIVE-VERIFY owed: texture paint
smoothness at 2048² on real GPU (only jsdom-measured). CamoPanel still via TopBar Paint→Camo.

## Plan
Wave 1 (disjoint files): Connect screen (1-4); 3D lighting (22-23); Shared reverts (5,13,14,21).
Wave 2 (disjoint files): Decal editor (9,10,11,15,16); Faceplate (6,7); Skin/texture (17,18,19).
Defer + scope separately: item 8/20 unify editors (major).
Then build + deploy; user verifies on the deployed build (they test the taskbar build, not dev).
